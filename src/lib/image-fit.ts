/** Top-anchored cover: fills the frame but keeps the upper part of the photo visible (banners, heroes). */
export const coverImageTopClass = "object-cover object-top";

export const coverImageTopPosition = "center top";

/** Shows the full image inside the frame (may letterbox). */
export const containImageClass = "object-contain object-center";

export const containImagePosition = "center center";
